# HivePitch

Hivesurf influencer marketing proposal deck (static HTML).

## GitHub Pages

After pushing, enable **Settings → Pages → Build and deployment → Branch: `main` / folder: `/ (root)`**.  
The deck loads at **`/hivesurf-proposal.html`**. The site root redirects there via `index.html`.
